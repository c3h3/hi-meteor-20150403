head
  title Hi Meteor! Title!

body
  +test

  
template(name="test")
  each rangeArray
    +testTemplate

template(name="testTemplate")  
  h2 ({{this}}) Hi Meteor! (h2 tag)
  p ({{this}}) Hi Meteor! (p tag)
